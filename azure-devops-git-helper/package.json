{
  "name": "azure-devops-git-helper",
  "version": "1.0.0",
  "description": "Chrome extension: generate PR descriptions from commit messages on Azure DevOps",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "type-check": "tsc --noEmit",
    "package": "npm run build && zip -r git-helper-ado.zip manifest.json icons/ dist/ popup/popup.html index.html -x '*.DS_Store'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
